<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <p class="navbar-brand">Pierre's Treats</p>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          @Html.ActionLink("Home", "Index", "Home", new {@class="nav-link"})
        </li>
        <li class="nav-item">
          @Html.ActionLink("Treats", "Index", "Treats", new {@class="nav-link"})
        </li>
        <li class="nav-item">
          @Html.ActionLink("Flavors", "Index", "Flavors", new {@class="nav-link"})
        </li>
        @if (User.Identity.IsAuthenticated)
        {
          <li class="navbar-nav">
            @Html.ActionLink("Logout", "Logout", "Account")
          </li>
        }
        else
        {
          <li class="navbar-nav">
            @Html.ActionLink("Login", "Login", "Account")
          </li>
          <li class="navbar-nav">
            @Html.ActionLink("Create Account", "Register", "Account")
          </li>
        }
      </ul>
    </div>
  </div>
</nav>